# 実装計画書 (plan.md) - 履歴画面ヘッダー改善

## スプリント1：CSSによるレイアウト修正

### タスク
1.  **[実装] style.css の修正**
    - `.history-filters` に `flex-wrap: wrap` を追加。
    - 各要素 (`input`, `select`) の `flex` プロパティを調整し、スマホ時に適切に広がるようにする。
    - モバイル用のメディアクエリ内での微調整。
2.  **[検証] 動作確認**
    - ブラウザのデベロッパーツール（スマホモード）で、期間・口座・カテゴリのセレクトボックスが全て表示され、操作可能であることを確認。

## スケジュール
- 即時実施。
