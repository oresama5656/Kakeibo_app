# リデザイン実装計画 (plan.md) - 日本向けカードスタイル

## スプリント1：カードスタイル・ヘッダーの実装

### タスク
1.  **[CSS] style.css の全面改修**
    - `.history-header` をカード形式に変更。
    - 入力要素 (`input`, `select`) に直接スタイルを当てるのではなく、それらを包む「カードコンテナ」を作成。
    - 指定された画像に近い配色（ブルーグレー系）を定義。
2.  **[JS] history.js のHTML構造改修**
    - フィルター要素を `.filter-card` クラスでラップ。
    - 現在の期間や資産合計をヘッダー上部に追加表示。
3.  **[検証] UI/UX 動作確認**
    - 日本のユーザーが「使いやすい」「綺麗」と感じる質感になっているか検証。

## スケジュール
- 即時実施。
