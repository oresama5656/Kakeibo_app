# 分析画面リデザイン実装計画 (plan.md)

## スプリント1：分析画面ヘッダーと全体のプレミアム化

### タスク
1.  **[CSS] style.css のプレミアム化（分析画面フォーカス）**
    - `.analysis-nav-header` などの、分析画面専用のプレミアムスタイルを定義。
    - 「深縹（ふか縹）」基調のグラデーションカードの定義。
    - 各種ボタン（`filter-chip-v2`, `type-btn-v2`）を Apple純正風のセグメントコントロールにリデザイン。
2.  **[JS] UIHelper.js のリデザイン実装**
    - `renderPLContent` の構造を全面的に書き換え。
    - サマリーカードを最上部に、フィルタ設定をその下に控えめに配置。
3.  **[JS] analysis.js のクラス調整**
    - `premium-mode` の演出強化。

## スケジュール
- フェーズ1: 基本レイアウトの整理（即時）
- フェーズ2: 配色・影・アニメーションの微調整

## 注意事項
- 既存のフィルタリング計算（`PeriodManager` 等）は維持し、表示構造（HTML/CSS）のみを刷新する。
