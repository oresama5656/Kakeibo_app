# 実装計画書 (plan.md) - カテゴリ表示ロジックの改善

## スプリント1：表示ロジックの修正と検証

### タスク
1.  **[実装] renderIconGrid の改修**
    - `displayItems` の算出ロジックを修正。ピン留めがない場合は選択中アイテムのみ（または空）にする。
    - 展開ボタンの表示フラグを `all.length > 0` （または適切な閾値）に基づくように変更。
2.  **[検証] 動作確認**
    - ピン留め0件時の表示確認。
    - カテゴリ選択時の表示確認。
    - 既存のピン留めありパターンの退行テスト。

## スケジュール
- 指定なし。即時実装可能。
